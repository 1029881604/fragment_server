<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	
	<!-- orm映射文件配置  配置mapper在spring中配置扫描即可
	但要求mapper接口文件必须和mapper.xml映射文件同名且在同一个包下
	<mappers>
		<package name="person.hwc.ssm.mapper"/>
	</mappers> -->
	
</configuration>